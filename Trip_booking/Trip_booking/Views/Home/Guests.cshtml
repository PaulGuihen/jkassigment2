@model IEnumerable<Trip_booking.Models.Guest>

@{
    ViewBag.Title = "Guests";
}

<h2>Guests</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Guest_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LegID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Guest_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LegID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GuestID }) |
            @Html.ActionLink("Details", "Details", new { id=item.GuestID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GuestID })
        </td>
    </tr>
}

</table>
