@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

<div class="col-md-6 col-md-offset-3 well-lg well spaceIt">
    </div>
                    @using (Html.BeginForm("Create", "Home", FormMethod.Post, new { @class = "form" }))
                    {
                        @Html.AntiForgeryToken()
                        @Html.ValidationSummary(true)

                        <h2>Create New Student</h2>
                        <h4>Data entered here is posted back to the controller that sent it. From there the Repository is asked to add the new Student to the database. It is after this that we have an ID for the student as this is created by SQL Server.</h4>
                        <div class="row">
                            <div class="col-xs-6 col-md-6 spaceIt">
                                @*@Html.EditorFor(model => model.LastName)*@
                                <input type="text" name="Guest_Name" value="" class="form-control input-lg" placeholder="Guest_Name" />
                                @*@Html.ValidationMessageFor(model => model.LastName)*@
                            </div>
                            </div>
                        <div>
                        </div>
                         <div class="col-xs-6 col-md-6 spaceIt">
                                <input class="btn btn-primary" type="submit" value="Create Guest"/>
                            </div>
                        <div class="col-md-6 col-md-offset-3">
                    @Html.ActionLink("Back to List", "Index", null, null, new { @class = "btn btn-warning" })
                </div>
                    }
                        @section Scripts {
    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.4.4/jquery.min.js"           type="text/javascript"></script>
    <script src="//ajax.googleapis.com/ajax/libs/jqueryui/1.8.9/jquery-ui.min.js"      type="text/javascript"></script>
    <script src="//ajax.aspnetcdn.com/ajax/jQuery.Validate/1.7/jQuery.Validate.min.js" type="text/javascript"></script>
    <script src="//ajax.aspnetcdn.com/ajax/mvc/3.0/jquery.validate.unobtrusive.min.js" type="text/javascript"></script>
    <script>
        toastr.info('Are you the 6 fingered man?');
    </script>
}