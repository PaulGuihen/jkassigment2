@model IEnumerable<Trip_booking.Models.Legs>

@{
    ViewBag.Title = "Legs";
}

<h2>Legs</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Start_location)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Finish_location)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Start_Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Finish_Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TripID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Start_location)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Finish_location)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Start_Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Finish_Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TripID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LegID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LegID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LegID })
        </td>
    </tr>
}

</table>
