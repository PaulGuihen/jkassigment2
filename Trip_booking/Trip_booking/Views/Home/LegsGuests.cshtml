@model IEnumerable<Trip_booking.Models.legs_guest>

@{
    ViewBag.Title = "LegsGuests";
}

<h2>LegsGuests</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LegID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GuestID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LegID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GuestID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
